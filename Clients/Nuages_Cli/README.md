### Nuages_Cli

To setup the client: 
```
npm install
```

To run the client: 

```
./nuages_cli.sh  - Connect to http://localhost:3030
or
./nuages_cli.sh http(s)://host:port - Connect to a remote host

```
